# `src`
This is where all the codes and stuffs go

## Directories
Below is all the directories list inside `src`


### `src/routes`
Where all the routes go

### `src/store`
Database storing handler

### `src/templates`
XML and actual server stuffs, taken from LittleBigArchive's LBPK & MNR Packet Captures

### `src/utils`
Utility and helper stuff


## Codes
Below is all the code list inside `src`


### `src/db.js`
Database handler, uses `pg` (postgresql) though

### `src/index.js`
The code that wires (almost) everything to become one LittleBig Family

(this readme was designed to mimic `InsightGit/karting-archive`'s direction style)